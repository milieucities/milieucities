<div id="users">
    <table>
      <thead>
        <tr>
          <th>First Name</th>
          <th>Last Name</th>
          <th>Full Name</th>
          <th>Email</th>
          <th>Signed up</th>
          <th>Receive Newsletter</th>
        </tr>
      </thead>

      <tbody>
        <% @users.each do |user| %>
        <tr>
          <td><%= user.first_name || '-'  %></td>
          <td><%= user.last_name || '-' %></td>
          <td><%= user.profile.try(:name) || '-' %></td>
          <td><%= user.email %></td>
          <td><%= user.created_at %></td>
          <td><%= user.profile.try(:receive_newletter) %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
</div>
